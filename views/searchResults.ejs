<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/customStyling.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>This the search bar...yay</title>
</head>

<body>
  <!-- Navbar -->
  <%- include('partials/navbar', { user: user }) %>

  <div class="container mt-4">
    <h2 class="text-center mb-4" style="color: #801408">Search Results for "<%= query %>"</h2>

    <% if (recipes.length > 0) { %>
      <div class="row">
        <% recipes.forEach(recipe => { %>
          <div class="col-md-3 mb-4">
            <div class="card"
              style="background-color: #fffaec; border-radius: 15px; box-shadow: 0 4px 8px rgba(0,0,0,0.1); max-width: 1000px;">
              <a href="/recipe/<%= recipe.recipeId %>">
                <img src="/images/recipeImages/<%= recipe.recipeImage %>" class="card-img-top"
                  alt="<%= recipe.recipeTitle %>" />
              </a>
              <div class="card-body">
                <h5 class="card-title" style="color: #801408">
                  <a href="/recipe/<%= recipe.recipeId %>" style="text-decoration: none; color: inherit;">
                    <%= recipe.recipeTitle %>
                  </a>
                </h5>
                <p class="card-text"><%= recipe.recipeDescription.substring(0, 100) %>...</p>
                <a href="/recipe/<%= recipe.recipeId %>" class="btn btn-yellow2 btn-sm mt-2">View</a>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <p class="text-center">No recipes are found for "<%= query %>".</p>
    <% } %>
  </div>
</body>

</html>