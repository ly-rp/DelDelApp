<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Recipe - DelDelApp</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/customStyling.css">
</head>
<body>
    <div class="container mt-5">
        <!-- Header Section -->
        <header class="d-flex justify-content-between align-items-center mb-4">
            <h1>Delete Recipe</h1>
        </header>

        <!-- Confirmation Message Section -->
        <div class="alert alert-warning">
            <h3>Are you sure you want to delete the following recipe?</h3>
            <p><strong>Recipe Name:</strong> <%= recipeTitle %></p>
            <p><strong>Category:</strong> <%= category %></p>
            <p><strong>Description:</strong> <%= recipeDescription %></p>
        </div>

        <!-- Delete Confirmation Form -->
        <form action="/admin/delete/<%= recipeId %>" method="POST">
            <div class="d-flex justify-content-between">
                <button type="submit" class="btn btn-danger">Yes, Delete</button>
                <a href="/admin/recipes" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </div>

    <!-- Bootstrap JS and Dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>